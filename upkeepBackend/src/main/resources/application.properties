# Your API port
server.port=8080

# --- MySQL datasource (backend running on your Mac) ---
spring.datasource.url=jdbc:mysql://localhost:3307/upkeep?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=rootpass
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# (optional safety net; not required if URL works)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Make sure to include the trailing slash!
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-6swto1ndp4wqynaq.us.auth0.com/
# This tells Spring *who* the tokens must be for
# It MUST EXACTLY match the Identifier from Step 1
spring.security.oauth2.resourceserver.jwt.audience=https://api.upkeep.com